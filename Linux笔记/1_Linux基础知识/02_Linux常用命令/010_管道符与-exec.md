在之前find查找指令的介绍中，我们提到了`-exec`选项，是将前一条命令的输出传递给后面一条命令去做处理。这篇文章就来正式介绍它的父亲大人——管道符“|”。

# 管道符 与 -exec

管道符使用竖杠表示——"|"。其作用就是将前面的命令的输出作为后面命令的输入，以流的形式进行传递。例如：

- 案例一：列出根目录下所有文件和目录并分屏显示

  ![image-20211215144957584](https://klelee-image.oss-cn-qingdao.aliyuncs.com/image/image-20211215144957584.png)

  会得到这样的一种输出方式，相比于一次性输出，方便阅读
